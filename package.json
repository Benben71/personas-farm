{
  "name": "personas-farm",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "dev": "cd personas-unified-demo && npm run dev",
    "build": "cd personas-unified-demo && npm run build",
    "clean": "rm -rf node_modules personas-unified-demo/node_modules"
  },
  "dependencies": {
    "@vercel/speed-insights": "^1.2.0"
  }
}
